// Thin adapter — actual data lives in /db/entertainment.json.
import db from "../../db/entertainment.json";

export const gamesPlayed = db.gamesPlayed;
export const webSeries = db.webSeries;
export const movies = db.movies;
export const anime = db.anime;
export const pairings = db.pairings;
