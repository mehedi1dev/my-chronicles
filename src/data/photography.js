// Thin adapter — actual data lives in /db/photography.json.
import db from "../../db/photography.json";

export const photoHighlights = db.photoHighlights;
export const photoSections = db.photoSections;
